name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'Dockerfile'
      - '.github/workflows/pipeline.yaml'
  pull_request:
    branches: ["main"]
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'Dockerfile'
      - '.github/workflows/pipeline.yaml'

jobs:
  unit_test:
    name: Unit Tests & Coverage
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Setup Go
        uses: actions/setup-go@v6.4.0
        with:
          go-version: "1.26.3"
          cache: true

      - name: Run Tests
        run: |
          go test -v -coverprofile=coverage.out ./...
          COVERAGE=$(go tool cover -func=coverage.out | grep total: | awk '{print $3}')
          echo "Total coverage: $COVERAGE"
          # Enforce 75% threshold (remove % for comparison)
          COV_VAL=$(echo $COVERAGE | sed 's/%//')
          awk -v cov="$COV_VAL" 'BEGIN { if (cov < 10) { print "Coverage too low: " cov " < 10"; exit 1 } }'

      - name: Update Coverage in README
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          COVERAGE=$(go tool cover -func=coverage.out | grep total: | awk '{print $3}')
          sed -i "s/- \*\*Code Coverage\*\*: .*/- \*\*Code Coverage\*\*: $COVERAGE (Target: 10%)/" README.md
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          git commit -m "docs: sync code coverage [skip ci]" || echo "No changes to commit"
          git push

  security_scan:
    name: Go Security Scan
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Setup Go
        uses: actions/setup-go@v6.4.0
        with:
          go-version: "1.26.3"
          cache: true

      - name: Cache Go Binaries
        id: cache-go-bin
        uses: actions/cache@v5.0.5
        with:
          path: ~/go/bin
          key: ${{ runner.os }}-go-bin-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-bin-

      - name: Install tools
        if: steps.cache-go-bin.outputs.cache-hit != 'true'
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          go install honnef.co/go/tools/cmd/staticcheck@latest
          go install github.com/securego/gosec/v2/cmd/gosec@latest

      - name: Run govulncheck
        run: govulncheck ./...

      - name: Run staticcheck
        run: staticcheck ./...

      - name: Run gosec
        run: gosec ./...

  docker_build_push:
    name: Build and Push Docker Image
    needs: [security_scan, unit_test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: KAnggara/DevOps/dockerbuild@v0.1.7
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

  tag_release:
    name: Create Release Tag
    needs: [docker_build_push]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0

      - name: Bump version and push tag
        id: tag_version
        uses: anothrNick/github-tag-action@1.75.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WITH_V: true
          DEFAULT_BUMP: patch

      - name: Create Release
        uses: softprops/action-gh-release@v3.0.0
        with:
          tag_name: ${{ steps.tag_version.outputs.new_tag }}
          name: Release ${{ steps.tag_version.outputs.new_tag }}
          body: |
            Automated Release ${{ steps.tag_version.outputs.new_tag }}

            Changes in this version:
            - Build and push Docker image to GHCR
            - Automated version bump
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
